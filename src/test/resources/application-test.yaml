spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration

vetclinic:
  jwt:
    secret: test-only-secret-min-32-bytes!!!!
    expiration: PT1H
  clinic-timezone: UTC
  bootstrap:
    enabled: false
